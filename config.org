* Setup custom file
- Write customizations to a separate file =custom.el=.
:setup-custom:
#+begin_src emacs-lisp
  (setq custom-file (expand-file-name "custom.el" user-emacs-directory))
  (load custom-file t)
#+end_src
:END:
* Session defaults
** General
:defaults:
#+begin_src emacs-lisp
  ;; Make frame transparency overridable
  ;; (defvar my/frame-transparency '(90 . 90))

  ;; Default file permissions
  ;; (set-default-file-modes #o660)

  ;; Editing options
  (global-set-key (kbd "C-x h") 'mark-whole-buffer)
  (put 'downcase-region 'disabled nil)
  (put 'upcase-region 'disabled nil)
  (put 'narrow-to-region 'disabled nil) ;;select and edit region
  (delete-selection-mode t) ;; delete the region when typing
  (cua-mode 0) ;Disable cua mode

  ;; Use UTF8
  (setq locale-coding-system 'utf-8)
  (set-terminal-coding-system 'utf-8)
  (set-keyboard-coding-system 'utf-8)
  (set-selection-coding-system 'utf-8)
  (prefer-coding-system 'utf-8)

  ;; Answering just 'y' or 'n' will do
  (defalias 'yes-or-no-p 'y-or-n-p)

  ;; Keep all backup and auto-save files in one directory
  (setq backup-directory-alist '(("." . "~/.emacs.d/backups")))

  ;; Visual customizations
  (blink-cursor-mode -1) ;; disable blinking cursor
  (setq visible-bell nil) ;; disable visible bell
  ;; (setq-default indent-tabs-mode nil)
  (setq-default indicate-empty-lines t)
  (show-paren-mode t) ;; highlight matching parenthesis
  (column-number-mode t) ;; Show column number
  (global-visual-line-mode) ;; wrap text (justify)
  ;; Don't count two spaces after a period as the end of a sentence.
  ;; Just one space is needed.
  (setq sentence-end-double-space nil)

  ;; Make buffer's names unique by adding a path
  (setq uniquify-buffer-name-style 'forward)
  (put 'dired-find-alternate-file 'disabled nil)

  ;; Server start
  (load "server")
  (unless (server-running-p)
    (server-start)
    ;; (setq server-socket-dir "~/.emacs.d/server")
    )

  ;; Enable variable-pitch mode for text-mode
  (add-hook 'text-mode-hook #'variable-pitch-mode)

  ;; Only show high level warnings
  (setq warning-minimum-level :emergency)

  ;; revert buffer Keybinding
  (defun my-revert-buffer-no-confirm ()
    "Revert the current buffer without confirmation."
    (interactive)
    (revert-buffer :ignore-auto :noconfirm))

  (global-set-key (kbd "<f5>") 'my-revert-buffer-no-confirm)

  ;; Line numbers
  (add-hook 'prog-mode-hook #'display-line-numbers-mode)

  ;; Line spacing
  ;; (setq-default line-spacing 4)

  ;;  Load shell conf (.bashrc)
  ;; (use-package exec-path-from-shell
  ;;   :ensure t
  ;;   :defer t
  ;;   :init
  ;;   (when (memq window-system '(mac ns x))
  ;;     (exec-path-from-shell-initialize)))

  ;;Openwith
  ;; (use-package openwith
  ;;   :ensure t
  ;;   :config
  ;;   (setq openwith-associations '(("\\.pdf\\'" "atril" (file))))
  ;;   )

  ;; ;; -i gets alias definitions from .bash_profile
  ;; (setq shell-command-switch "-ic")
#+end_src
:END:
** Custom variables
- Links to configuration sections: [[* Python (Anaconda)][Pyhon (Anaconda)]] [[* Org-ref][Org-ref bibtex-path]] [[* Org-reveal][Org-reveal]] [[* Ansi-term][Ansi-term]]  [[* Org-roam][Org-roam]]
- If statements are used to set variables for
  1) Path to anaconda installation
     - Default: =~/.local/bin/miniconda3=
     - Custom: =/media/discs/shared/miniconda3=
  2) Path to bibtex files
     - One for a Linux installation in Spanish (searches for the folder =Documentos=)
     - Another for a Linux installation in English (searches for the folder =Documents=)
  3) Path to [[https://revealjs.com/][Reveal.js]] installation (Presentations)
     - Two different paths
- Additionally fixed- and variable-width font variables are defined
  - monotype: Courier-New
  - sans: Verdana
  - default: Courier-New
*Modify these accordingly*
:custom-vars:
#+begin_src emacs-lisp
  ;; Path to anaconda installation
  (defvar conda-dir
    (if (file-directory-p "/media/discs/shared/miniconda3")
        "/media/discs/shared/miniconda3"
      (if (file-directory-p "~/.local/bin/miniconda3")
          "~/.local/bin/miniconda3"
        "other")))

  (defvar my/anaconda-home conda-dir)
  (defvar my/anaconda-venv (concat conda-dir "/envs"))
  (defvar my/mmdc-path (concat conda-dir "/bin/mmdc"))

  ;; Bibtex paths and files
  (defvar my/docs-dir
    (if (file-directory-p "~/Documents")
        "~/Documents"
      (if (file-directory-p "~/Documentos")
          "~/Documentos"
        "other")))

  ;; mermaid-cli path
  ;; (setq ob-mermaid-cli-path  my/mmdc-path)

  ;; org-roam directory
  (setq my/org-roam-dir (concat my/docs-dir "/Org-files/Org-roam"))

  ;; org-noter directory
  (setq my/org-noter-dir (concat my/docs-dir "/Apuntes/Org_noter"))

  ;; Bibtex files
  (setq
   bibtex-completion-bibliography (concat my/docs-dir "/Refs/Bibtex/Working.bib")
   bibtex-completion-library-path (concat my/docs-dir "/Papers_Books/")
   bibtex-completion-notes-path (concat my/docs-dir "/Papers_Books/Summaries"))

  ;; ebib
  (setq my/ebib-search-dir (concat my/docs-dir "/Refs/Bibtex/"))
  (setq my/ebib-bibtex-files
      (list
       (concat my/docs-dir "/Refs/Bibtex/Articles_zotero.bib")
       (concat my/docs-dir "/Refs/Bibtex/Books_zotero.bib")))

  ;; org-reveal-path
  (defvar my/reveal-path
    (if (file-directory-p "/media/discs/shared/Cloned/reveal.js")
        "file:///media/discs/shared/Cloned/reveal.js"
      (if (file-directory-p "/media/particiones/Cloned/reveal.js")
          "file:///media/particiones/Cloned/reveal.js"
        "other")))
  (setq org-reveal-root my/reveal-path)

  ;; Path to bash (ansi term)
  (defvar my/my-term "/bin/bash")

  ;; Fonts
  (defvar my/mono-font "Courier-New")
  (defvar my/sans-font "Verdana")
  (defvar my/default-font "Courier-New")
#+end_src
:END:
** Startup time
:startup-time:
#+begin_src emacs-lisp
  ;;Tweak garbage collection
  ;; The default is 800 kilobytes.  Measured in bytes.
  ;; (setq gc-cons-threshold (* 50 1000 1000))

  (defun my/display-startup-time ()
    (message "Emacs loaded in %s with %d garbage collections."
             (format "%.2f seconds"
                     (float-time
                      (time-subtract after-init-time before-init-time)))
             gcs-done))

  (add-hook 'emacs-startup-hook #'my/display-startup-time)
#+end_src
:END:
** Packages without configuration
*** Description
- [[https://github.com/magit/magit][magit]]: Git porcelain for Emacs
- [[https://github.com/larstvei/ox-gfm][ox-gfm]]: Export Org mode files to GitHub Flavored Markdown
- [[https://code.orgmode.org/bzg/org-mode][org]]: Emacs mode for notes, TODO lists, etc.
- [[https://github.com/auctex/auctex][auctex]]: Major mode for editing TeX files
- [[https://github.com/joostkremers/ebib][ebib]]: Emacs BibTeX database manager
- [[https://github.com/emacs-ess/ESS][ess]]: Emacs Speaks Statistics - for statistical programming
- [[https://github.com/rnkn/olivetti][olivetti]]: Emacs minor mode for distraction-free writing
- [[https://github.com/purcell/unfill][unfill]]: Emacs package for removing line breaks from paragraphs
- [[https://github.com/lassik/emacs-format-all-the-code][format-all]]: Format code in Emacs using external tools
- [[https://github.com/polymode/poly-R][poly-R]]: Emacs Org mode extension for working with R code
- [[https://github.com/polymode/poly-markdown][poly-markdown]]: Emacs package for poly-mode in Markdown
- [[https://github.com/bbatsov/crux][crux]]: Emacs utility functions for getting the most out of Emacs
- [[https://github.com/djvu-el/djvu.el][djvu]]: DjVu mode for Emacs
- [[https://github.com/wasamasa/nov.el][nov]]: Emacs package for reading EPUB files
- [[https://github.com/pythonic-emacs/anaconda-mode][anaconda-mode]]: Emacs minor mode for Python development
*** Installation
:install-packages:
#+begin_src emacs-lisp
  (setq ipackages '(magit ox-gfm org auctex ebib ess olivetti unfill format-all poly-R poly-markdown crux djvu nov anaconda-mode))
  ;; install packages
  (dolist (package ipackages)
      (straight-use-package package))
#+end_src
:END:
* Appearance
** Themes
[[[https://emacsthemes.com/][Emacs themes]]]
*** Preferred themes
**** Dark themes
- base16: base16-da-one-sea base16-tokyo-night-storm base16-everforest base16-railscasts base16-sandcastle
- ef-themes: ef-autumn ef-elea-dark ef-maris-dark ef-tritanopia-dark
- Independent: flatland darktooth-theme timu-spacegrey mellow-theme warm-night-theme jazz-theme blackboard-theme arjen-theme deep-blue-theme desert-theme  railscast robin-hood suscolors-theme flucui-themes labburn
**** Light themes
- built-in: dichromacy
- base16-themes: base16-github base16-dirtysea base16-equilibrium-gray-light
- modus-themes: modus-operandi
- ef-themes: ef-day ef-deuteranopia-light ef-elea-light ef-kassio ef-melissa-light ef-tritanopia-light
- cloud-theme majapahit andreas gtk-ide vim-colors
**** Write
- base16-themes: base16-grayscale-light base16-gruvbox-material-light-soft base16-rose-pine-dawn base16-tokyo-city-terminal-light
- almost-mono-themes: almost-mono-cream almost-mono-gray eink-theme
  - Independent: monotropic eink nord poet kingsajz
*** Install themes
:install-themes:
#+begin_src emacs-lisp
  ;;modus-themes base16-theme doom-themes
  (setq ithemes '(ef-themes doom-themes modus-themes
                            kaolin-themes material-theme
                            almost-mono-themes monotropic-theme))

  ;; install themes
  (dolist (theme ithemes)
    (straight-use-package theme))
#+end_src
:END:
*** Github only themes                         :commented:
:test:
#+begin_src emacs-lisp
  ;; (straight-use-package
  ;;  '(replace-colorthemes
  ;;    :type git
  ;;    :host github
  ;;    :repo "emacs-jp/replace-colorthemes"
  ;;    :files ("parus-theme.el" "snow-theme.el"
  ;;            "late-night-theme.el" "resolve-theme.el"))
  ;;  )
#+end_src
:END:
*** Functions to cycle over preferred themes
:cycle-through-themes:
#+begin_src emacs-lisp
  ;; List of favorite themes
  (setq my-dark-themes '("material" "doom-fairy-floss" "ef-maris-dark"
                         "doom-gruvbox" "kaolin-eclipse" "kaolin-temple" ))
  (setq my-light-themes '("doom-oksolar-light" "ef-duo-light" "ef-cyprus"
                          "kaolin-mono-light" "ef-melissa-light" "ef-arbutus"))
  (setq my-write-themes '("almost-mono-cream" "monotropic" "doom-homage-white"
                           "almost-mono-gray" ))
  ;; Cycle through themes
  (defun rotate-dark-themes ()
    (interactive)
    (dolist (theme custom-enabled-themes)
      (disable-theme theme))
    (setq my-dark-themes (append (cdr my-dark-themes) (list (car my-dark-themes))))
    (let ((theme (car my-dark-themes))
          (command (format "(load-theme '%s t)" (car my-dark-themes))))
      (message "Loading theme: %s" theme)
      ;; (message "Command: %s" command)
      (eval (read command))))

  (defun rotate-light-themes ()
    (interactive)
    (dolist (theme custom-enabled-themes)
      (disable-theme theme))
    (setq my-light-themes (append (cdr my-light-themes) (list (car my-light-themes))))
    (let ((theme (car my-light-themes))
          (command (format "(load-theme '%s t)" (car my-light-themes))))
      (message "Loading theme: %s" theme)
      ;; (message "Command: %s" command)
      (eval (read command))))

  (defun rotate-write-themes ()
    (interactive)
    (dolist (theme custom-enabled-themes)
      (disable-theme theme))
    (setq my-write-themes (append (cdr my-write-themes) (list (car my-write-themes))))
    (let ((theme (car my-write-themes))
          (command (format "(load-theme '%s t)" (car my-write-themes))))
      (message "Loading theme: %s" theme)
      ;; (message "Command: %s" command)
      (eval (read command))))

  ;; Asign key to rotate themes
  (global-set-key (kbd "<f12>") 'rotate-dark-themes)
  (global-set-key (kbd "<f9>") 'rotate-light-themes)
  (global-set-key (kbd "<f7>") 'rotate-write-themes)

  ;; Default theme
  (load-theme 'ef-arbutus t)
#+end_src
:END:
** Window size
:window-size:
#+begin_src emacs-lisp
  ;; Window height
  (add-to-list 'default-frame-alist '(height . 45))
  ;; Window Width
  (add-to-list 'default-frame-alist '(width . 90)) 
#+end_src
:END:
** Fonts
*** Fonts in org-mode
:Font-faces:
#+begin_src emacs-lisp
  (defun customize-org-faces ()
    "Customize org-faces"
    ;;Size for heading levels
    (dolist (face '((org-level-1 . 1.5)
                    (org-level-2 . 1.4)
                    (org-level-3 . 1.3)
                    (org-level-4 . 1.2)
                    (org-level-5 . 1.1)
                    (org-level-6 . 1.0)
                    (org-level-7 . 1.0)
                    (org-level-8 . 1.0)))
      (set-face-attribute (car face) nil
                          :inherit 'variable-pitch
                          :height (cdr face))
      )

    ;; Ensure that anything that should be fixed-pitch in Org files appears that way
    (set-face-attribute 'org-block nil    :foreground 'unspecified :inherit 'fixed-pitch)
    (set-face-attribute 'org-drawer nil    :inherit 'fixed-pitch)
    (set-face-attribute 'org-table nil    :inherit 'fixed-pitch)
    (set-face-attribute 'org-formula nil  :inherit 'fixed-pitch)
    (set-face-attribute 'org-code nil     :inherit '(shadow fixed-pitch))
    (set-face-attribute 'org-table nil    :inherit '(shadow fixed-pitch))
    (set-face-attribute 'org-verbatim nil :inherit '(shadow fixed-pitch))
    (set-face-attribute 'org-special-keyword nil :inherit '(font-lock-comment-face fixed-pitch))
    (set-face-attribute 'org-meta-line nil :inherit '(font-lock-comment-face fixed-pitch))
    (set-face-attribute 'org-checkbox nil  :inherit 'fixed-pitch)
    (set-face-attribute 'line-number nil :inherit 'fixed-pitch)
    (set-face-attribute 'line-number-current-line nil :inherit 'fixed-pitch)
    )
  (customize-org-faces)
#+end_src
:END:
*** Fontaine
[[[https://protesilaos.com/emacs/fontaine][Home]]] [[[https://www.youtube.com/watch?v=qR8JRYr4BKE][Youtube demo]]]
- Package to customize fonts (faces) written by Protesilaos Stavrou
- =Fontaine-presets= need more work (customize further)
:fontaine:
#+begin_src emacs-lisp
  (use-package fontaine
    :straight (fontaine
               :type git
               :host github
               :repo "emacs-straight/fontaine"
               :files ("*" (:exclude ".git")))
    :config
    (setq fontaine-latest-state-file
          (locate-user-emacs-file "fontaine-latest-state.eld"))
    (setq fontaine-presets
          '(
           (regular
             :default-height 130)
            (medium
             :default-height 150)
            (large
             :default-height 170)
            (regular-desktop
             :default-height 105)
            (medium-desktop
             :default-height 120)
            (large-desktop
             :default-height 135)
            (t
             :default-family "Courier New" ;;Roboto Mono
             :default-weight regular
             :fixed-pitch-family "Courier New" ;;Source Code Pro
             :fixed-pitch-weight regular
             :fixed-pitch-height 1.0
             :fixed-pitch-serif-family nil ; falls back to :default-family
             :fixed-pitch-serif-weight nil ; falls back to :default-weight
             :fixed-pitch-serif-height 1.0
             :variable-pitch-family "Verdana" ;;Nunito
             :variable-pitch-weight regular
             :variable-pitch-height 1.0
             :bold-family nil ; use whatever the underlying face has
             :bold-weight bold
             :italic-family nil
             :italic-slant italic
             :line-spacing 4))
          )
    )
  ;; Recover last preset or fall back to desired style from
  ;; `fontaine-presets'.
  (fontaine-set-preset (or (fontaine-restore-latest-preset) 'regular))

  ;; The other side of `fontaine-restore-latest-preset'.
  (add-hook 'kill-emacs-hook #'fontaine-store-latest-preset)

  (defvar after-enable-theme-hook nil
    "Normal hook run after enabling a theme.")

  (defun run-after-enable-theme-hook (&rest _args)
    "Run `after-enable-theme-hook'."
    (run-hooks 'after-enable-theme-hook))

  (advice-add 'enable-theme :after #'run-after-enable-theme-hook)

  ;; (add-hook 'after-enable-theme-hook #'fontaine-apply-current-preset)
  (add-hook 'after-enable-theme-hook #'customize-org-faces)
#+end_src
:END:
*** Font scaling
:font-scaling:
#+begin_src emacs-lisp
  (use-package default-text-scale
    :straight (default-text-scale :type git
              :flavor melpa
              :host github
              :repo "purcell/default-text-scale")
    :config
    (global-set-key (kbd "C-M-+") 'default-text-scale-increase)
    (global-set-key (kbd "C-M--") 'default-text-scale-decrease))
  (define-key ctl-x-map [(control ?0)] 'zoom-in/out)
#+end_src
:END:
** Icons
*** Nerd Icons
:nerd-icons:
#+begin_src emacs-lisp
  (use-package nerd-icons
    :straight (nerd-icons :type git
                          :flavor melpa
                          :files (
                                  :defaults "data" "nerd-icons-pkg.el")
                          :host github
                          :repo "rainstormstudio/nerd-icons.el")

      )
  (use-package nerd-icons-dired
    :straight (nerd-icons-dired :type git :host github :repo "rainstormstudio/nerd-icons-dired")
    :hook (dired-mode . nerd-icons-dired-mode))
#+end_src
:END:
** Modeline
- =Modeline= is the region on a window where the file name and the active modes (among other information) are displayed.
- Available modelines [[https://github.com/seagle0128/doom-modeline][Doom-modeline]], [[https://github.com/TheBB/spaceline][Spaceline]], [[https://github.com/gexplorer/simple-modeline][Simple modeline]], [[https://github.com/Malabarba/smart-mode-line][Smartmodeline]], [[https://github.com/dbordak/telephone-line][Telephone-Line]]
*** Doom-modeline
[[[https://github.com/seagle0128/doom-modeline][GitHub]]]
**** Requirements
To show the icons it may be necessary to install =NerdFontsSymbolsOnly= from [[https://github.com/ryanoasis/nerd-fonts][nerd-fonts]]. See this post on [[https://www.reddit.com/r/emacs/comments/v5adgc/comment/kg1yi0i/?utm_source=share&utm_medium=web3x&utm_name=web3xcss&utm_term=1&utm_content=share_button][Reddit]] and this [[https://www.geekbits.io/how-to-install-nerd-fonts-on-linux/][blog post]] on how to install nerd fonts
#+begin_src bash :eval never-export
  # Download and install fonts
  myFont="NerdFontsSymbolsOnly.zip"
  wget https://github.com/ryanoasis/nerd-fonts/releases/download/v3.2.1/$myFont
  unzip $myFont
  mv $myFont/*ttf ~/.local/share/fonts

  # Update font database
  fc-cache -fv
#+end_src
**** Configuration
:doom-modeline:
#+begin_src emacs-lisp
  ;; Customize modeline
  ( use-package doom-modeline
    :straight (doom-modeline :type git
                             :flavor melpa
                             :host github
                             :repo "seagle0128/doom-modeline")
    :init
    (doom-modeline-mode 1)
    ;; Display time in the mode line
    (display-time-mode 1)

    ;; Optional: customize the format
    (setq display-time-format "%H:%M") ;; 24-hour format
    (setq display-time-default-load-average nil) ;; Remove system load

    :config
    ;; Enable display time in doom-modeline
    (setq doom-modeline-time t)
    (setq doom-modeline-time-icon t)

    :custom (
             (doom-modeline-height 33)
             (setq doom-modeline-window-width-limit 65)
             (setq doom-modeline-buffer-file-name-style 'auto)
             (setq doom-modeline-buffer-name t)
             )
    )
#+end_src
:END:
** Change appearance
*** Resize windows
- Keybindings to resize windows.
:resize-widows:
#+begin_src emacs-lisp
  (bind-key "s-C-<left>"  'shrink-window-horizontally)
  (bind-key "s-C-<right>" 'enlarge-window-horizontally)
  (bind-key "s-C-<down>"  'shrink-window)
  (bind-key "s-C-<up>" 'enlarge-window)
#+end_src
:END:
*** Split windows
- Split windows and switch
:split-window:
#+begin_src emacs-lisp
  ;;Define functions to split a window vertically or horizontally
  (defun vsplit-other-window ()
    "Splits the window vertically and switches to that window."
    (interactive)
    (split-window-vertically)
    (other-window 1 nil))
  (defun hsplit-other-window ()
    "Splits the window horizontally and switches to that window."
    (interactive)
    (split-window-horizontally)
    (other-window 1 nil))

  ;; Define keybindings for using the above functions
  (bind-key "C-x 2" 'vsplit-other-window)
  (bind-key "C-x 3" 'hsplit-other-window)
#+end_src
:END:
*** Change size of split windows (Zoom)
[[[https://github.com/cyrus-and/zoom][Zoom GitHub]]]
When using a split frame, change the size of the windows based on which one is active
:zoom:
#+begin_src emacs-lisp
  (use-package zoom
    :straight (zoom
               :type git
               :flavor melpa
               :host github
               :repo "cyrus-and/zoom")
    :init
    (custom-set-variables
     '(zoom-size '(0.618 . 0.618)))
    )
#+end_src
:END:
* Navigating
** Move between windows
Define Keybindings to move between windows
:win-move:
#+begin_src emacs-lisp
  ;; (windmove-default-keybindings 'super)
  ;; (add-hook 'org-shiftup-final-hook 'windmove-up)
  ;; (add-hook 'org-shiftleft-final-hook 'windmove-left)
  ;; (add-hook 'org-shiftdown-final-hook 'windmove-down)
  ;; (add-hook 'org-shiftright-final-hook 'windmove-right)
  (global-set-key [s-left] 'windmove-left) ;move to left window
  (global-set-key [s-right] 'windmove-right) ;move to right window
  (global-set-key [s-up] 'windmove-up) ;move to upper window
  (global-set-key [s-down] 'windmove-down) ;move to lower window
#+end_src
:END:
** Window-purpose
Gives a better control where Emacs places buffers. [[[https://github.com/bmag/emacs-purpose][GitHub]]]
Start with =M-x purpose-mode=
Set a buffer for a defined purpose =C-c d= =purpose-toggle-window-purpose-dedicated=
:purpose:
#+begin_src emacs-lisp
  (use-package window-purpose
      :straight (window-purpose :type git
                              :flavor melpa
                              :files (
                                      :defaults "layouts" "window-purpose-pkg.el")
                              :host github
                              :repo "bmag/emacs-purpose")
      :config
      ;; (add-to-list 'purpose-user-mode-purposes '(org-mode . org))
      ;; (add-to-list 'purpose-user-mode-purposes '(ess-mode . R))
      ;; (purpose-compile-user-configuration)
      (purpose-mode)
      )
#+end_src
:END:
** Manage/Save window configuration (Eyebrowse)
[[[https://depp.brause.cc/eyebrowse/][Eyebrowse Home]]]
Helps manage window configurations (Different number of divisions/dimensions).
- To create a second configuration =C-c C-w 2=. Rearrange the windows as necessary
- To switch to the first configuration =C-c C-w 1=
:eyebrowse:
#+begin_src emacs-lisp
  (use-package eyebrowse
    :straight (eyebrowse :type git
                          :flavor melpa
                          :repo "https://depp.brause.cc/eyebrowse.git")
    :config
    (eyebrowse-mode))
#+end_src
:END:
** Bookmarks
:bookmarks:
#+begin_src emacs-lisp
  (use-package bm
    :straight (bm :type git
            :flavor melpa
            :host github
            :repo "joodland/bm")
    :ensure t
    :demand t

    :init
    ;; restore on load (even before you require bm)
    (setq bm-restore-repository-on-load t)


    :config
    ;; Allow cross-buffer 'next'
    (setq bm-cycle-all-buffers t)

    ;; where to store persistant files
    (setq bm-repository-file "~/.emacs.d/bm-repository")

    ;; save bookmarks
    (setq-default bm-buffer-persistence t)

    ;; Loading the repository from file when on start up.
    (add-hook 'after-init-hook 'bm-repository-load)

    ;; Saving bookmarks
    (add-hook 'kill-buffer-hook #'bm-buffer-save)

    ;; Saving the repository to file when on exit.
    ;; kill-buffer-hook is not called when Emacs is killed, so we
    ;; must save all bookmarks first.
    (add-hook 'kill-emacs-hook #'(lambda nil
                                   (bm-buffer-save-all)
                                   (bm-repository-save)))

    ;; The `after-save-hook' is not necessary to use to achieve persistence,
    ;; but it makes the bookmark data in repository more in sync with the file
    ;; state.
    (add-hook 'after-save-hook #'bm-buffer-save)

    ;; Restoring bookmarks
    (add-hook 'find-file-hooks   #'bm-buffer-restore)
    (add-hook 'after-revert-hook #'bm-buffer-restore)

    ;; The `after-revert-hook' is not necessary to use to achieve persistence,
    ;; but it makes the bookmark data in repository more in sync with the file
    ;; state. This hook might cause trouble when using packages
    ;; that automatically reverts the buffer (like vc after a check-in).
    ;; This can easily be avoided if the package provides a hook that is
    ;; called before the buffer is reverted (like `vc-before-checkin-hook').
    ;; Then new bookmarks can be saved before the buffer is reverted.
    ;; Make sure bookmarks is saved before check-in (and revert-buffer)
    (add-hook 'vc-before-checkin-hook #'bm-buffer-save)

    ;; Set keybindings to set bookmarks in the buffer
    :bind (("C-c <right>" . bm-next)
           ("C-c <left>" . bm-previous)
           ("C-c b" . bm-toggle))
    )

  ;; Color for bookmarks
  ;; (set-face-attribute 'bm-face nil :background "GreenYellow" :foreground 'unspecified)
#+end_src
:END:
** Recent files (Recentf)
- Create a list of recently opened files
  :recentf:
  #+begin_src emacs-lisp
    (use-package recentf
      :straight t
      :config
      (recentf-mode t)
      ;; (setq recentf-max-menu-items 15)
      ;; (setq recentf-max-saved-items 15)
      ;; :bind
      ;; (("C-x C-r" . recentf-open-files))
      )
  #+end_src
  :END:
** File navigation/management
*** Neotree
[[[https://github.com/jaypei/emacs-neotree][GitHub]]]
:neotree:
#+begin_src emacs-lisp
  (use-package neotree
    :straight (neotree :type git
                       :flavor melpa
                       :files (
                               :defaults "icons" "neotree-pkg.el")
                       :host github
                       :repo "jaypei/emacs-neotree")
    :bind
    (:map global-map
  	([f8] . 'neotree-toggle)
  	)
    :config
    (setq neo-theme (if (display-graphic-p) 'nerd-icons))
    )
#+end_src
:END:
*** Treemacs
[[[https://github.com/Alexander-Miller/treemacs\]\[https://github.com/Alexander-Miller/treemacs][GitHub]]]
A tree layout file explorer for Emacs
:treemacs:
#+begin_src emacs-lisp
  (use-package treemacs
    :straight (treemacs :type git
                        :flavor melpa
                        :files (:defaults "Changelog.org" "icons" "src/elisp/treemacs*.el" "src/scripts/treemacs*.py" (:exclude "src/extra/*") "treemacs-pkg.el")
                        :host github
                        :repo "Alexander-Miller/treemacs")
    :defer t
    :config
    (progn
      (setq treemacs-follow-after-init          t
            treemacs-width                      35
            treemacs-indentation                2
            treemacs-git-integration            t
            treemacs-collapse-dirs              3
            treemacs-silent-refresh             nil
            treemacs-change-root-without-asking nil
            treemacs-sorting                    'alphabetic-desc
            treemacs-show-hidden-files          t
            treemacs-never-persist              nil
            treemacs-is-never-other-window      nil
            treemacs-goto-tag-strategy          'refetch-index)

      (treemacs-follow-mode t)
      (treemacs-filewatch-mode t))
    :bind
    (:map global-map
          ([f6]        . treemacs)
          ;; ([f9]        . treemacs-projectile-toggle)
          ;; ("<C-M-tab>" . treemacs-toggle)
          ("M-0"       . treemacs-select-window)
          ("C-c 1"     . treemacs-delete-other-windows)
          ))
  ;; (use-package treemacs-projectile
  ;;   :defer t
  ;;   :ensure t
  ;;   :config
  ;;   (setq treemacs-header-function #'treemacs-projectile-create-header)
  ;;   )
#+end_src
:END:
*** Dired
**** Configuration
:dired-config:
#+begin_src emacs-lisp
  (use-package dired
    :ensure nil
    :init
    ;; Hide details such as permissions or modification date
    (add-hook 'dired-mode-hook 'dired-hide-details-mode)
    :commands
    (dired dired-jump)

    ;;Jumps to the parent level
    :bind (("C-x C-j" . dired-jump))
    :config
    (setq dired-recursive-copies 'always)
    (setq dired-recursive-deletes 'always)
    (setq delete-by-moving-to-trash t)
    (setq dired-dwim-target t) ;try to guess where to copy...

    :hook
    (dired-mode . dired-hide-details-mode)
    :custom
    ;; Sorting folders and files
    ((dired-listing-switches "-agho --group-directories-first"))
    )

  ;; Use icons for folders and files
  ;; (use-package all-the-icons-dired
  ;;   :after dired
  ;;   :straight (all-the-icons-dired :type git
  ;;                         	 :flavor melpa
  ;;                         	 :host github
  ;;                         	 :repo "wyuenho/all-the-icons-dired")
  ;;   :hook (dired-mode . all-the-icons-dired-mode)
  ;;   )

  ;;Dired narrow allows filter by pattern, regex
  (use-package dired-narrow
    :after dired
    :straight (dired-narrow :type git
                            :flavor melpa
                            :files ("dired-narrow.el" "dired-narrow-pkg.el")
                            :host github
                            :repo "Fuco1/dired-hacks")
    :config
    (bind-key "C-c C-n" #'dired-narrow)
    (bind-key "C-c C-f" #'dired-narrow-fuzzy)
    (bind-key "C-x C-N" #'dired-narrow-regexp)
    )

  ;; Allows open a folder with tab as a subtree
  (use-package dired-subtree
    :after dired
    :straight (dired-subtree :type git
                             :flavor melpa
                             :files ("dired-subtree.el" "dired-subtree-pkg.el")
                             :host github
                             :repo "Fuco1/dired-hacks")
    :config
    (bind-key "<tab>" #'dired-subtree-toggle dired-mode-map)
    (bind-key "<backtab>" #'dired-subtree-cycle dired-mode-map)
    )

  ;; Prevent opening multiple buffers
  (use-package dired-single
    :after dired
    :straight (dired-single
               :type git
               :flavor melpa
               :host codeberg
               :repo "amano.kenji/dired-single")
    :config
    (define-key dired-mode-map [return] 'dired-single-buffer)
    (define-key dired-mode-map [mouse-1] 'dired-single-buffer-mouse)
    (define-key dired-mode-map "^" 'dired-single-up-directory)
    )

  ;; dired-interface to trash bin
  (use-package trashed
    :straight (trashed :type git
                       :host github
                       :repo "shingo256/trashed")
    :commands (trashed)
    :config
    (setq trashed-action-confirmer 'y-or-n-p)
    (setq trashed-use-header-line t)
    (setq trashed-sort-key '("Date deleted" . t))
    (setq trashed-date-format "%Y-%m-%d %H:%M:%S")
    )
  ;; (use-package dired-open
  ;;   :after dired
  ;;   :straight (dired-open
  ;;     	     :type git
  ;;     	     :flavor melpa
  ;;     	     :files ("dired-open.el" "dired-open-pkg.el")
  ;;     	     :host github
  ;;     	     :repo "Fuco1/dired-hacks")

  ;;   :commands (dired dired-jump)
  ;;   :config
  ;;   ;; Doesn't work as expected!
  ;;   (add-to-list 'dired-open-functions #'dired-open-xdg t)
  ;;   (setq dired-open-extensions '(("png" . "eom")
  ;;                                 ("pdf" . "atril")
  ;;                                 ))
  ;;   )

  ;; (use-package dired-hide-dotfiles
  ;;   :ensure t
  ;;   :hook (dired-mode . dired-hide-dotfiles-mode))
#+end_src
:END:
*** Zoxide.el
:zoxide-config:
#+begin_src emacs-lisp
  (use-package zoxide
    :straight `(zoxide.el :type git :host github :repo "emacsmirror/zoxide" :files (:defaults))
    :config
    (defun dired-jump-with-zoxide (&optional other-window)
      (interactive "P")
      (zoxide-open-with nil (lambda (file) (dired-jump other-window file)) t)))
#+end_src
:END:
** Imenu
[[[https://www.emacswiki.org/emacs/ImenuMode][Wiki]]]
Produces menus for accessing locations in documents.
:imenu-keybindings:
#+begin_src emacs-lisp
  (use-package imenu-list
    :straight (imenu-list :type git
                          :host github
                          :repo "bmag/imenu-list")
    :bind(
          ("C-c i" . imenu-list-smart-toggle)
          )
    )
#+end_src
:END:
** Smoothscrolling
This makes it so C-n-ing and C-p-ing won’t make the buffer jump
around so much.
#+begin_src emacs-lisp
  (use-package smooth-scrolling
    :straight (smooth-scrolling :type git
                                :flavor melpa
                                :host github
                                :repo "aspiers/smooth-scrolling")
    :config
    (smooth-scrolling-mode))
#+end_src
** Ace-jump-mode
Allows you to quickly jump to any visible point in the buffer by typing a few characters. When activated, it displays letters next to each word or character, and you can jump to a specific location by pressing the corresponding letter.
#+begin_src emacs-lisp
  (use-package ace-jump-mode
    :straight (ace-jump-mode :type git
                              :flavor melpa
                              :host github
                              :repo "winterTTr/ace-jump-mode")
    :bind
    ("C-c SPC" . ace-jump-mode)
    )
#+end_src
* Buffers
** List buffers (Ibuffer)
[[[https://www.emacswiki.org/emacs/IbufferMode][Wiki]]]
Improved version of list-buffers.
:ibuffer:
#+begin_src emacs-lisp
  ;; set as default
  (defalias 'list-buffers 'ibuffer)

  ;; source: http://ergoemacs.org/emacs/emacs_buffer_management.html
  (add-hook 'dired-mode-hook 'auto-revert-mode)

  ;; Also auto refresh dired, but be quiet about it
  (setq global-auto-revert-non-file-buffers t)
  (setq auto-revert-verbose nil)

  ;; hide buffers that begin with *
  (require 'ibuf-ext)
  (add-to-list 'ibuffer-never-show-predicates "^\\*")
  ;;     (mapcar #'regexp-quote '("^\\*helm.*" "^\\*Ebib.*")))
  ;; (add-to-list 'ibuffer-never-show-predicates "^\\*helm.*")
  ;; (add-to-list 'ibuffer-never-show-predicates "^\\*Ebib.*")
  ;; (add-to-list 'ibuffer-never-show-predicates "^\\*swiper.*")
  ;; (add-to-list 'ibuffer-never-show-predicates "^\\*Custom.*")
  ;; (add-to-list 'ibuffer-never-show-predicates "^\\*Packages.*")
  ;; (add-to-list 'ibuffer-never-show-predicates "^\\*Flymake.*")
  ;; (add-to-list 'ibuffer-never-show-predicates "^\\*magit.*")
  ;; (add-to-list 'ibuffer-always-show-predicates "^\\*Python\\*")

  ;;hide Filename process cycling with backtick
  (setq ibuffer-format-save ibuffer-formats)
  (setq ibuffer-formats (append ibuffer-formats '((mark modified " " name))))

  (global-set-key (kbd "C-x C-b") 'ibuffer)
  (setq ibuffer-saved-filter-groups
        (quote (("default"
                 ("dired" (mode . dired-mode))
                 ("Web" (or (name . "^.*html$")
                            (name . "^.*css$")
                            (name . "^.*js$")
                            ))
                 ("org" (name . "^.*org$"))
                 ("markdown" (name . "^.*md$"))
                 ("yaml / toml" (or (name . "^.*yaml$")
                                    (name . "^.*toml$")
                                    ))
                 ("magit" (mode . magit-mode))
                 ;; ("web" (or (mode . web-mode)
                 ;;            (mode . js2-mode)))
                 ("shell" (or (mode . eshell-mode)
                              (mode . shell-mode)))
                 ("programming" (or
                                 (mode . python-mode)
                                 (mode . c++-mode)
                                 ))
                 ("Bash" (or (name . "^.*sh$")))
                 ("TeX"(or (name . "^.*tex$")
                           (name . "^.*sty$")
                             ))
                 ("txt"(or (name ."^.*txt$")))
                 ("pdf"(or (name ."^.*pdf$")))
                 ("R" (or (name . "^.*R$")
                          (name . "^.*Rmd$")
                          (mode . ess-mode)))
                 ("Lisp" (or(mode . emacs-elisp-mode)))
                 ("emacs" (or
                           (name . "^\\*scratch\\*$")
                           (name . "^\\*Messages\\*$")))
                 ))))
  (add-hook 'ibuffer-mode-hook
            (lambda ()
              (ibuffer-auto-mode 1)
              (ibuffer-switch-to-saved-filter-groups "default")))

  ;; don't show these
  ;; Don't show filter groups if there are no buffers in that group
  (setq ibuffer-show-empty-filter-groups nil)

  ;; Don't ask for confirmation to delete marked buffers
  (setq ibuffer-expert t)
#+end_src
:END:
** Scratch buffer
Convenient package to create *scratch* buffers that are based on the current buffer’s major mode. This is more convenient than manually creating a buffer to do some scratch work or reusing the initial *scratch* buffer.
#+begin_src emacs-lisp
  (use-package scratch
    :straight (scratch :type git
                       :flavor melpa
                       :files ("scratch.el" "scratch-pkg.el")
                       :host codeberg
                       :repo "emacs-weirdware/scratch")
    :commands scratch)
#+end_src
* Completion
** Corfu (COmpletion in Region FUnction)
[[[https://github.com/minad/corfu][GitHub]]]
Enhances completion at point with a small completion popup.
#+begin_src emacs-lisp
  (use-package corfu
    :straight (corfu
               :type git
               :flavor melpa
               :files (:defaults "extensions/corfu-*.el" "corfu-pkg.el")
               :host github
               :repo "minad/corfu")
    :config
    (setq corfu-cycle t                ; Enable cycling for `corfu-next/previous'
          corfu-auto t                 ; Enable auto completion
          ;; corfu-auto-delay 60.0        ; Delay before auto-completion shows up
          corfu-separator ?\s          ; Orderless field separator
          corfu-quit-at-boundary nil   ; Never quit at completion boundary
          corfu-quit-no-match t        ; Quit when no match
          corfu-preview-current nil    ; Disable current candidate preview
          corfu-preselect-first nil    ; Disable candidate preselection
          corfu-on-exact-match nil     ; Configure handling of exact matches
          corfu-echo-documentation nil ; Disable documentation in the echo area
          corfu-scroll-margin 5)       ; Use scroll margin

    (global-corfu-mode)
    )
#+end_src

#+begin_src emacs-lisp
  ;; TAB cycle if there are only few candidates
  (setq completion-cycle-threshold 3)

  ;; Emacs 28: Hide commands in M-x which do not apply to the current mode.
  ;; Corfu commands are hidden, since they are not supposed to be used via M-x.
  ;; (setq read-extended-command-predicate
        ;; #'command-completion-default-include-p)

  ;; Enable indentation+completion using the TAB key.
  ;; completion-at-point is often bound to M-TAB.
  (setq tab-always-indent 'complete)
#+end_src
** Cape (Completion At Point Extensions)
[[[https://github.com/minad/cape][GitHub]]]
Extends the capabilities of completion frameworks like Corfu and Company, providing additional completion sources and utilities for enhanced user experience.
#+begin_src emacs-lisp
  (use-package cape
    :straight (cape
               :type git
               :flavor melpa
               :host github
               :repo "minad/cape")
    :bind
    (("C-c p p" . completion-at-point) ;; capf
     ("C-c p t" . complete-tag)        ;; etags
     ("C-c p d" . cape-dabbrev)        ;; or dabbrev-completion
     ("C-c p h" . cape-history)
     ("C-c p f" . cape-file)
     ("C-c p k" . cape-keyword)
     ;; ("C-c p s" . cape-elisp-symbol)
     ;; ("C-c p e" . cape-elisp-block)
     ("C-c p a" . cape-abbrev)
     ("C-c p l" . cape-line)
     ("C-c p w" . cape-dict)
     ("C-c p \\" . cape-tex)
     ("C-c p _" . cape-tex)
     ("C-c p ^" . cape-tex)
     ;; ("C-c p &" . cape-sgml)
     )
    :init
    ;; Add to the global default value of `completion-at-point-functions' which is
    ;; used by `completion-at-point'.  The order of the functions matters, the
    ;; first function returning a result wins.  Note that the list of buffer-local
    ;; completion functions takes precedence over the global list.
    (add-to-list 'completion-at-point-functions #'cape-dabbrev)
    (add-to-list 'completion-at-point-functions #'cape-file)
    ;; (add-to-list 'completion-at-point-functions #'cape-elisp-block)
    ;; (add-to-list 'completion-at-point-functions #'cape-history)
    (add-to-list 'completion-at-point-functions #'cape-keyword)
    (add-to-list 'completion-at-point-functions #'cape-tex)
    ;;(add-to-list 'completion-at-point-functions #'cape-sgml)
    ;;(add-to-list 'completion-at-point-functions #'cape-rfc1345)
    ;;(add-to-list 'completion-at-point-functions #'cape-abbrev)
    ;;(add-to-list 'completion-at-point-functions #'cape-dict)
    ;;(add-to-list 'completion-at-point-functions #'cape-elisp-symbol)
    ;;(add-to-list 'completion-at-point-functions #'cape-line)
    :config
    (defun toggle-cape-dabbrev ()
      "Toggle the `cape-dabbrev` completion-at-point function."
      (interactive)
      (if (memq 'cape-dabbrev completion-at-point-functions)
          (setq completion-at-point-functions
                (remove 'cape-dabbrev completion-at-point-functions))
        (add-to-list 'completion-at-point-functions 'cape-dabbrev))
      (message "cape-dabbrev %s"
               (if (memq 'cape-dabbrev completion-at-point-functions)
                   "enabled"
                 "disabled")))
    )
#+end_src
** Orderless
[[[https://github.com/oantolin/orderless][GitHub]]]
Allow completion based on space-separated tokens, out of order.
#+begin_src emacs-lisp
  (use-package orderless
    :straight (orderless :type git
                         :flavor melpa
                         :host github
                         :repo "oantolin/orderless")
    :config
    (setq completion-styles '(substring orderless basic)
          orderless-component-separator 'orderless-escapable-split-on-space
          read-file-name-completion-ignore-case t
          read-buffer-completion-ignore-case t
          completion-ignore-case t)
    )
#+end_src
** Hydra
#+begin_src emacs-lisp
    (use-package hydra
      :straight  (hydra :type git
                              :files (
                                      :defaults (
                                                 :exclude "lv.el") "hydra-pkg.el")
                              :host github
                              :repo "abo-abo/hydra")
      :config
      (defhydra hydra-diff-hl (:hint nil :exit nil)
      "
  Diff-HL:
    _n_: Next hunk     _p_: Previous hunk
    _s_: Stage hunk    _r_: Revert hunk
    _q_: Quit
  "
      ("n" diff-hl-next-hunk "Next hunk")
      ("p" diff-hl-previous-hunk "Previous hunk")
      ("s" diff-hl-stage-current-hunk "Stage hunk")
      ("r" diff-hl-revert-hunk "Revert hunk")
      ("q" nil "Quit" :exit t))

      )
#+end_src
** Yasnippet
:yasnippet:
#+begin_src emacs-lisp
  (use-package yasnippet
    :straight (yasnippet :type git
                         :flavor melpa
                         :files ("yasnippet.el" "snippets" "yasnippet-pkg.el")
                         :host github
                         :repo "joaotavora/yasnippet")
    :defer t
    :config
    (use-package yasnippet-snippets
      :straight (yasnippet-snippets :type git
                                    :flavor melpa
                                    :files ("*.el" "snippets" ".nosearch" "yasnippet-snippets-pkg.el")
                                    :host github
                                    :repo "AndreaCrotti/yasnippet-snippets"))
    (yas-reload-all)
    :hook
    ((org-mode LaTeX-mode web-mode python-mode ess-r-mode c++-mode cc-mode sh-mode) . yas-minor-mode)
    )
#+end_src
:END:
* Minibuffer
** Consult
[[[https://github.com/minad/consult][GitHub]]]
Replace some of emacs functions with their consult equivalent.
For the [[help:consult-goto-line][consult-goto-line]] and ~consult-line~ commands, we define our owns with live preview (independently of the [[help:consult-preview-key][consult-preview-key]])
#+begin_src emacs-lisp
  (use-package consult
  :straight (consult :type git
                     :flavor melpa
                     :host github
                     :repo "minad/consult")
  :init
  ;; Disable live preview globally but enable it in specific cases
  (setq consult-preview-key nil)

  ;; Custom functions with local preview configuration
  (defun my/consult-line ()
    "Consult line with live preview."
    (interactive)
    (let ((consult-preview-key 'any)
          (mini-frame-resize 'grow-only)) ;; Keep mini-frame resizing under control
      (consult-line)))

  (defun my/consult-goto-line ()
    "Consult goto line with live preview."
    (interactive)
    (let ((consult-preview-key 'any))
      (consult-goto-line)))

  :bind
  (("C-c y" . yank-from-kill-ring)
   ("C-x C-r" . consult-recent-file)
   ("C-x C-o" . consult-outline)
   ("C-x b" . consult-buffer)
   ("C-x M-h" . consult-history)
   ("C-c r" . consult-ripgrep)
   ("C-s" . my/consult-line)
   ("M-g g" . my/consult-goto-line)
   ("M-g M-g" . my/consult-goto-line)))
#+end_src
** Vertico
[[[https://github.com/minad/vertico][GitHub]]]
Provides a performant and minimalistic vertical completion UI based on the default completion system but aims to be highly flexible, extensible and modular.
Bind =shift-tab= for completion
Completion-at-point and completion-in-region (see
https://github.com/minad/vertico#completion-at-point-and-completion-in-region)
Prefix the current candidate
(See https://github.com/minad/vertico/wiki#prefix-current-candidate-with-arrow)
See https://kristofferbalintona.me/posts/vertico-marginalia-all-the-icons-completion-and-orderless/#vertico
See https://github.com/minad/vertico/issues/145
#+begin_src emacs-lisp
  (use-package vertico
    :straight (vertico :type git
                       :flavor melpa
                       :files (:defaults "extensions/vertico-*.el" "vertico-pkg.el")
                       :host github
                       :repo "minad/vertico")
    :init
    ;; Formatting candidate lines in the minibuffer
    (defun minibuffer-format-candidate (orig cand prefix suffix index _start)
      "Customize the candidate display in the minibuffer."
      (let ((prefix (if (= vertico--index index) " > " "   ")))
        (funcall orig cand prefix suffix index _start)))

    (advice-add #'vertico--format-candidate
                :around #'minibuffer-format-candidate)

    :config
    ;; Vertico configuration
    (setq vertico-resize nil        ; Do not resize the Vertico minibuffer window
          vertico-count 8           ; Show a maximum of 8 candidates
          vertico-count-format nil  ; No prefix with number of entries
          truncate-lines t)         ; Avoid line-wrapping in Vertico

    ;; Use `consult-completion-in-region' if Vertico is enabled.
    ;; Otherwise use the default `completion--in-region' function.
    (setq completion-in-region-function
          (lambda (&rest args)
            (apply (if vertico-mode
                       #'consult-completion-in-region
                     #'completion--in-region)
                   args)))

    (vertico-mode)
    )
#+end_src
** Embark
#+begin_src emacs-lisp
  (use-package embark
    :straight (embark :type git
                      :flavor melpa
                      :files ("embark.el" "embark-org.el" "embark.texi" "embark-pkg.el")
                      :host github
                      :repo "oantolin/embark")
    :bind
    (("C-c a" . embark-act))
    :init
    (setq prefix-help-command #'embark-prefix-help-command)
    :config
      (add-to-list 'display-buffer-alist
               '("\\`\\*Embark Collect \\(Live\\|Completions\\)\\*"
                 nil
                 (window-parameters (mode-line-format . none))))
    )

  (use-package embark-consult
    :straight (embark-consult :type git
                              :flavor melpa
                              :files ("embark-consult.el" "embark-consult-pkg.el")
                              :host github
                              :repo "oantolin/embark")
    :after
    (embark consult)
    )
#+end_src
** Marginalia
[[[https://github.com/minad/marginalia][GitHub]]]
#+begin_src emacs-lisp
  (use-package marginalia
    :straight (marginalia :type git
                            :flavor melpa
                            :host github
                            :repo "minad/marginalia")
    :config
    (setq-default marginalia--ellipsis "…"    ; Nicer ellipsis
                  marginalia-align 'right     ; right alignment
                  marginalia-align-offset -1) ; one space on the right
    (marginalia-mode)
    )
#+end_src
** Which Key
#+begin_src emacs-lisp
  (use-package which-key
    :straight (which-key :type git
                         :flavor melpa
                         :host github
                         :repo "justbur/emacs-which-key")
    ;;:defer t
    :config
    (which-key-mode))
#+end_src
* Pdf
** pdf-tools
Use pdf-tools as default pdf viewer in emacs. Customize navigation and highlighting
#+begin_src emacs-lisp
  (use-package pdf-tools
    :straight (pdf-tools :type git
                         :flavor melpa
                         :files (:defaults "README" ("build" "Makefile") ("build" "server") "pdf-tools-pkg.el")
                         :host github
                         :repo "vedang/pdf-tools")
    :mode ("\\.pdf\\'" . pdf-view-mode)
    :commands (pdf-view-mode pdf-tools-install)
    :config
    ;; (pdf-tools-install)

    ;; Set default display size to fit width
    (setq-default pdf-view-display-size 'fit-width)
    (setq pdf-view-resize-factor 1.1)

    ;; Automatically activate annotations after creation
    (setq pdf-annot-activate-created-annotations t)

    ;; Keybindings for common annotations in pdf-view-mode
    (let ((map pdf-view-mode-map))
      (define-key map (kbd "C-s") 'isearch-forward)
      (define-key map (kbd "h") 'pdf-annot-add-highlight-markup-annotation)
      (define-key map (kbd "u") 'pdf-annot-add-underline-markup-annotation)
      (define-key map (kbd "t") 'pdf-annot-add-text-annotation)
      (define-key map (kbd "y") 'pdf-annot-add-squiggly-markup-annotation)
      (define-key map (kbd "_") 'pdf-annot-add-strikeout-markup-annotation)
      (define-key map (kbd "D") 'pdf-annot-delete))

    ;; Default properties for annotations
    (setq pdf-annot-default-annotation-properties
          '((t (label . "ssl"))
            (text (color . "khaki1") (icon . "Note"))
            (highlight (color . "snow2"))
            (underline (color . "dark cyan"))
            (squiggly (color . "orange red"))
            (strike-out (color . "red"))))

    ;; Recent color history for annotations
    (setq pdf-annot-color-history
          '("khaki1" "powder blue" "thistle" "DarkSeaGreen2"
            "light salmon" "snow2" "dark cyan" "orange red")))
#+end_src
* Org-mode
** Aspect
*** Org-bullets
Change the aspect of the bullets used in headings
:bullets:
#+begin_src emacs-lisp
  (use-package org-bullets
    :straight (org-bullets :type git
                           :flavor melpa
                           :host github
                           :repo "integral-dw/org-bullets")
    ;; :after org
    :hook
    (org-mode . (lambda () (org-bullets-mode 1)))
    :custom
    ;; Other styles
    ;; (org-bullets-bullet-list '("◉" "●" "▷" "▶" "●" "✸"))
    ;; (org-bullets-bullet-list '("㊀" "㊁" "㊂" "㊃" "㊄" "㊅"))
    ;; (org-bullets-bullet-list '("➀" "➁" "➂" "➃" "➄" "➅"))
    (org-bullets-bullet-list '("🅐" "🅑" "🅒" "🅓" "🅔" "🅕"))
    )
#+end_src
:END:
** Org babel languages
:load-languages:
#+begin_src emacs-lisp
  (use-package ob-restclient
    :straight (ob-restclient :type git
                             :flavor melpa
                             :host github
                             :repo "alf/ob-restclient.el")
    ;; :after org
    :config
    (org-babel-do-load-languages
     'org-babel-load-languages
     '((python . t)
       (latex . t)
       (R . t)
       (lisp . t)
       (shell . t)
       (awk . t)
       ;; (mermaid . t)
       ;; (scheme . t)
       ;; (yaml . t)
       ;; (restclient . t)
       ;;   (C . t)
       ;;   (calc . t)
       ;;   (java . t)
       ;;   (ruby . t)
       ;;   (sqlite . t)
       ;;   (js . t)
       ))
    (defun my-org-confirm-babel-evaluate (lang body)
      "Do not confirm evaluation for these languages."
      (not (or ;; (string= lang "C")
            ;;	(string= lang "java")
            (string= lang "python")
            (string= lang "emacs-lisp")
            ;; (string= lang "sqlite")
            )))
    (setq org-confirm-babel-evaluate 'my-org-confirm-babel-evaluate)
    ;; (setq org-src-tab-acts-natively t)
    )
#+end_src
:END:
** Org templates
:code-block-templates:
#+begin_src emacs-lisp
  (use-package org-tempo
    :ensure nil
    ;; :mode ("\\.org" . org-mode)
    ;; :after org
    :init
    (add-to-list 'org-structure-template-alist '("lp" . "src emacs-lisp"))
    (add-to-list 'org-structure-template-alist '("py" . "src python"))
    (add-to-list 'org-structure-template-alist '("sh" . "src sh"))
    (add-to-list 'org-structure-template-alist '("r" . "src R"))
    (add-to-list 'org-structure-template-alist '("b" . "src bash"))
    (add-to-list 'org-structure-template-alist '("md" . "src markdown"))
    (add-to-list 'org-structure-template-alist '("tx" . "src tex"))
    )
#+end_src
:END:
** Ox-hugo
:ox-hugo:
#+begin_src emacs-lisp
  (use-package ox-hugo
    :straight (ox-hugo :type git
                       :flavor melpa
                       :host github
                       :repo "kaushalmodi/ox-hugo")
    :after ox
    :config
    ;; Colored text for ox-hugo
    (defun my/code ()
      "Add a 'mark' macro to the current region (for Hugo)"
      (interactive)
      (if (region-active-p)
          (progn
            (goto-char (region-end))
            (insert ")}}}")
            (goto-char (region-beginning))
            (insert "{{{code("))))
    ;; mark text for ox-hugo
    (defun my/mark ()
      "Add a 'mark' macro to the current region (for Hugo)"
      (interactive)
      (if (region-active-p)
          (progn
            (goto-char (region-end))
            (insert "</mark>@@")
            (goto-char (region-beginning))
            (insert "@@html:<mark>"))))
    )
#+end_src
:END:
** Org exporting
*** Pandoc exporter
Pandoc converts between a huge number of different file formats.
#+begin_src emacs-lisp
  (use-package ox-pandoc
    :no-require t
    ;; :defer 10
    :straight (ox-pandoc :type git
                         :flavor melpa
                         :host github
                         :repo "emacsorphanage/ox-pandoc"))
#+end_src
*** Latex exporting
#+begin_src emacs-lisp
  (setq org-latex-pdf-process (list "latexmk -xelatex %f"))
  ;; (setq org-latex-listings 'minted
  ;;    org-latex-packages-alist '(("" "minted")))
  ;;(setq org-latex-pdf-process (list "arara %f"))
#+end_src

Latex export configuration [[https://orgmode.org/worg/org-tutorials/org-latex-export.html][Org-tutorials]]
It is necessary to load [[https://superuser.com/questions/896741/how-do-i-configure-org-latex-classes-in-emacs][Ox-latex]]
#+begin_src emacs-lisp
  ;; Define class definitions with custom commands
  (defvar custom-latex-classes
    '(("plain"
       "\\documentclass{article}
      \\usepackage{hyperref}
              [NO-DEFAULT-PACKAGES]
              [NO-PACKAGES]
        [NO-EXTRA]
      "
       ("\\section{%s}" . "\\section*{%s}")
       ("\\subsection{%s}" . "\\subsection*{%s}")
       ("\\subsubsection{%s}" . "\\subsubsection*{%s}")
       ("\\paragraph{%s}" . "\\paragraph*{%s}")
       ("\\subparagraph{%s}" . "\\subparagraph*{%s}"))

      ("basic"
       "\\documentclass{article}
      \\usepackage{hyperref}
              [NO-DEFAULT-PACKAGES]
      "
       ("\\section{%s}" . "\\section*{%s}")
       ("\\subsection{%s}" . "\\subsection*{%s}")
       ("\\subsubsection{%s}" . "\\subsubsection*{%s}")
       ("\\paragraph{%s}" . "\\paragraph*{%s}")
       ("\\subparagraph{%s}" . "\\subparagraph*{%s}"))

      (
       "custom-report"
       "\\documentclass{report}
  \\usepackage{custom_packages}
  [NO-DEFAULT-PACKAGES]
  [EXTRA]
  "
       ("\\chapter{%s}" . "\\chapter{%s}")
       ("\\section{%s}" . "\\section*{%s}")
       ("\\subsection{%s}" . "\\subsection*{%s}")
       ("\\subsubsection{%s}" . "\\subsubsection*{%s}")
       ("\\paragraph{%s}" . "\\paragraph*{%s}")
       ("\\subparagraph{%s}" . "\\subparagraph*{%s}"))

      (
       "custom-article"
       "\\documentclass{article}
  \\usepackage{custom_packages}
  [NO-DEFAULT-PACKAGES]
  [EXTRA]
  "
       ("\\section{%s}" . "\\section{%s}")
       ("\\subsection{%s}" . "\\subsection{%s}")
       ("\\subsubsection{%s}" . "\\subsubsection{%s}")
       ("\\paragraph{%s}" . "\\paragraph{%s}")
       ("\\subparagraph{%s}" . "\\subparagraph{%s}"))
      ;; Add more custom classes as needed
      ))


  ;; Define function to test for each class
  (defun ensure-latex-classes (class-definitions)
    "Ensure that the given CLASS-DEFINITIONS are defined in `org-latex-classes`."
    (dolist (class-def class-definitions)
      (let ((class (car class-def))
            (commands (cdr class-def)))
        (unless (cl-find class org-latex-classes :key #'car :test #'equal)
          (setq org-latex-classes
                (append org-latex-classes
                        (list (cons class commands))))))))

  (with-eval-after-load 'ox-latex
    (ensure-latex-classes custom-latex-classes)
    )
#+end_src
*** Markdown
#+begin_src emacs-lisp
  (eval-after-load "org"
    '(require 'ox-gfm nil t))
#+end_src
** Org-noter
Take notes from =pdf= documents (books, papers) in org mode
- Documentation ([[https://github.com/weirdNox/org-noter][GitHub]])
:org-noter:
#+begin_src emacs-lisp
  (use-package org-noter
    :straight (org-noter
               :type git
               :flavor melpa
               :files ("*.el" "modules" (:exclude "*-test-utils.el" "*-devel.el") "org-noter-pkg.el")
               :host github
               :repo "org-noter/org-noter")
    :commands (org-noter)  ;; Load only when org-noter is called
    :config
    '(org-noter-notes-search-path my/org-noter-dir)
    )
#+end_src
:END:
** Org-reveal
:org-reveal:
#+begin_src emacs-lisp
  (use-package ox-reveal
    :straight (ox-reveal :type git 
                         :flavor melpa 
                         :host github 
                         :repo "yjwen/org-reveal")
    ;; Load only when these commands are called
    :commands (org-reveal-export-to-html
               org-reveal-publish-to-reveal)
    ;; :init
    ;; (require 'ox-reveal)
    )
#+end_src
:END:
** Org-ref
:org-ref:
#+begin_src emacs-lisp
  (use-package org-ref
    :straight (org-ref :type git
                       :flavor melpa
                       :files (
                               :defaults "org-ref.org" "org-ref.bib" "citeproc" "org-ref-pkg.el")
                       :host github
                       :repo "jkitchin/org-ref")
  ;; :after org
  :init
  ;;Configuration to allow use of org-ref with ox-hugo
  (with-eval-after-load 'ox
    (defun my/org-ref-process-buffer--html (backend)
      ;;"Preprocess org-ref' citations to HTML format.
      ;;Do this only if the export backend is html' or a derivative of that."

      (when (org-export-derived-backend-p backend 'html)
        (org-ref-process-buffer 'html)))
    (add-to-list 'org-export-before-parsing-hook #'my/org-ref-process-buffer--html))

    :config
    (setq

     bibtex-completion-notes-template-multiple-files "* ${author-or-editor}, ${title}, ${journal}, (${year}) :${=type=}: \n\nSee [[cite:&${=key=}]]\n"

   bibtex-completion-additional-search-fields '(keywords)
   bibtex-completion-pdf-field "File"
   bibtex-completion-display-formats
   '((article       . "${=has-pdf=:1}${=has-note=:1} ${year:4} ${author:36} ${title:*} ${journal:40}")
     (inbook        . "${=has-pdf=:1}${=has-note=:1} ${year:4} ${author:36} ${title:*} Chapter ${chapter:32}")
     (incollection  . "${=has-pdf=:1}${=has-note=:1} ${year:4} ${author:36} ${title:*} ${booktitle:40}")
     (inproceedings . "${=has-pdf=:1}${=has-note=:1} ${year:4} ${author:36} ${title:*} ${booktitle:40}")
     (t             . "${=has-pdf=:1}${=has-note=:1} ${year:4} ${author:36} ${title:*}"))
   bibtex-completion-pdf-open-function
   (lambda (fpath)
     (call-process "open" nil 0 nil fpath))
   )
  (custom-set-faces
   '(org-ref-cite-face ((t (:inherit link :foreground unspecified)))))

    ;;Keybindings for inserting reference
    (define-key bibtex-mode-map (kbd "s-]") 'org-ref-bibtex-hydra/body)
    (define-key org-mode-map (kbd "C-c ]") 'org-ref-insert-link)
    (define-key org-mode-map (kbd "s-[") 'org-ref-insert-link-hydra/body)
    (setq org-ref-open-pdf-function 'org-pdfview-open)
    )
#+end_src
:END:
** Org-roam
:config-roam:
#+begin_src emacs-lisp
  (use-package org-roam
    :straight (org-roam :type git
                        :flavor melpa
                        :files (:defaults "extensions/*" "org-roam-pkg.el")
                        :host github
                        :repo "org-roam/org-roam")
    :custom
    (org-roam-directory my/org-roam-dir)
    (org-roam-capture-templates
     '(
       ;; Default template
       ("d" "default" plain "%?"
        :if-new (file+head "%<%Y%m%d%H%M%S>-${slug}.org"
                           "#+title: ${title}\n#+filetags: \n\n")
        :unnarrowed t)

       ;; Personal template
       ("p" "personal" plain
        "\n* Description\n%?\n\n* Back-links\n"
        :if-new (file+head "%<%Y%m%d%H%M%S>-${slug}.org"
                           "#+title: ${title}\n#+filetags: \n\n")
        :unnarrowed t)

       ;; notes template
       ("n" "notes" plain
        (file "~/Templates/org-roam-notes-template.org")
        :if-new (file+head "%<%Y%m%d%H%M%S>-${slug}.org"
                           "#+title: ${title}\n#+filetags: \n\n")
        :unnarrowed t)
       ))

    :bind (("C-c n l" . org-roam-buffer-toggle)
           ("C-c n f" . org-roam-node-find)
           ("C-c n g" . org-roam-graph)
           ("C-c n i" . org-roam-node-insert)
           ("C-c n c" . org-roam-capture)
           ;; Dailies
           ("C-c n j" . org-roam-dailies-capture-today))
    :config
    ;; If you're using a vertical completion framework, you might want a more informative completion interface
    (setq org-roam-node-display-template (concat "${title:*} " (propertize "${tags:10}" 'face 'org-tag)))
    (org-roam-db-autosync-mode)
    (org-roam-setup)
    )
#+end_src
:END:
*** org-roam-ui
:org-roam-ui:
#+begin_src emacs-lisp
  (use-package org-roam-ui
    :straight
    (:host github :repo "org-roam/org-roam-ui" :branch "main" :files ("*.el" "out"))
    :after org-roam
    :defer t
    :config
    (setq org-roam-ui-sync-theme t
          org-roam-ui-follow t
          org-roam-ui-update-on-save t
          org-roam-ui-open-on-start t))
#+end_src
:END:
** Mermaid                                     :commented:
Interface to [[https://github.com/mermaid-js/mermaid-cli][mermaid-cli]] for generating diagrams using org-mode. [[https://mermaid.js.org/][Mermaid]] is a 'JavaScript based diagramming and charting tool that renders Markdown-inspired text definitions to create and modify diagrams dynamically.'
It requires the installation of =mermaid-cli= via node.js. =npm install -g @mermaid-js/mermaid-cli=.
:mermaid:
#+begin_src emacs-lisp
  ;; (use-package ob-mermaid
  ;;   :straight (ob-mermaid :type git
  ;; 			:flavor melpa
  ;; 			:host github
  ;; 			:repo "arnm/ob-mermaid")
  ;;   ;; :after org
  ;;   )
#+end_src
:END:
** Commented
* Coding
** General
:general-options:
#+begin_src emacs-lisp
  (electric-pair-mode 1)
#+end_src
:END:
** Format code (Format-all)
- [[[https://ianyepan.github.io/posts/format-all/][Home]]] [[[https://github.com/lassik/emacs-format-all-the-code][Github]]]
/"Lets you auto-format source code in many languages using the same command for all languages"/
- You have to make sure that you have the external formatter programs (See [[* Some external formaters][some external formaters]] [[https://github.com/lassik/emacs-format-all-the-code?tab=readme-ov-file#supported-languages][supported languages]])
- To format, =M-x format-all-region-or-buffer=
- To auto-format on save use the minor mode, =mode format-all-mode=
*** Some external formaters
| Language | Package                                                   | Notes                                                  |
|----------+-----------------------------------------------------------+--------------------------------------------------------|
| R        | styler                                                    | =install.packages("styler")=                           |
| Bash     | [[https://tracker.debian.org/pkg/golang-mvdan-sh][shfmt]] | =sudo apt install golang-mvdan-sh-dev=                 |
|          | [[https://github.com/lovesegfault/beautysh][beautysh]]    | =pip install beautysh=                                 |
| Python   | various                                                   | =pip3 install black jedi autopep8 flake8 ipython yapf= |
| Html     | [[http://www.html-tidy.org/][tidy]]                       | =sudo apt install tidy=                                |
| Latex    | auctex                                                    | Part of [[https://tug.org/texlive/][TexLive]]          |
*** Configuration
#+begin_src emacs-lisp
  (use-package format-all
    :straight (format-all :type git
                          :host github
                          :repo "lassik/emacs-format-all-the-code")

    :commands format-all-mode
    :hook
    ((ess-mode python-mode LaTeX-mode
               Shell-script-mode sh-mode) . format-all-mode)
    ;; :config
    ;; (setq-default format-all-formatters
    ;;               '(("formatR" format-all-r-format-buffer)))
    )
#+end_src
** Check code (Flycheck)
[[[https://www.flycheck.org/en/latest/][Home]]]
On-the-fly syntax checking. "/It uses various syntax checking and linting tools to automatically check the contents of buffers while you type, and reports warnings and errors directly in the buffer, or in an optional error list/".
- Requires the installation of syntax checker programs (See [[#sec:requirements-flycheck][examples]] [[https://www.flycheck.org/en/latest/languages.html][supported languages]])
*** Some syntax checker programs
| Language   | Package                                                 | Notes                                         |
|------------+---------------------------------------------------------+-----------------------------------------------|
| R          | [[https://lintr.r-lib.org/][lintr]]                     | =install.packages("lintr")=                   |
| Awk        | gawk                                                    | =sudo apt install gawk=                       |
| Bash       | [[https://github.com/koalaman/shellcheck/][shellcheck]] | =sudo apt install shellcheck=                 |
| Html       | [[http://www.html-tidy.org/][tidy]]                     | =sudo apt install tidy=                       |
| JavaScript | [[https://jshint.com/][jshint]]                         | =npm install -g jshint=                       |
| Python     | various                                                 | =pip3 install pylint flake8=                  |
|            |                                                         | =sudo apt install pylint=                     |
| Latex      | lacheck                                                 | Part of [[https://tug.org/texlive/][TexLive]] |
*** Configuration
#+begin_src emacs-lisp
  (use-package flycheck
    :straight (flycheck :type git
                        :flavor melpa
                        :host github
                        :repo "flycheck/flycheck")
    :hook
    ((python-mode sh-mode ess-r-mode LaTeX-mode) . flycheck-mode)
    )
#+end_src
** Languages
*** Python (Anaconda)
Change the path for the anaconda (miniconda) installation accordingly
:config:
#+begin_src emacs-lisp
  ;; Install and configure Conda
  (use-package conda
    :straight (conda :type git
                     :flavor melpa
                     :host github
                     :repo "necaris/conda.el")
    :init
    (setq conda-anaconda-home my/anaconda-home
          conda-env-home-directory my/anaconda-venv
          conda-env-autoactivate-base t)
    :config
    ;; Activate the base environment and initialize conda features
    (conda-env-activate "base")
    (conda-env-initialize-interactive-shells)
    (conda-env-initialize-eshell)
    :hook
    ((python-mode . anaconda-mode)
     (python-mode . anaconda-eldoc-mode)))

  ;; Set up Elpy with Conda integration
  (use-package elpy
    :straight (elpy :type git
                    :flavor melpa
                    :files ("*.el" "NEWS.rst" "snippets" "elpy" "elpy-pkg.el")
                    :host github
                    :repo "jorgenschaefer/elpy")
    :init
    (elpy-enable)
    (setq python-shell-interpreter "python"
          elpy-rpc-completion-backend "corfu"
          elpy-rpc-virtualenv-path  'current
          python-shell-virtualenv-root my/anaconda-venv
          elpy-rpc-virtualenv-path  my/anaconda-venv)
    ;; (setq elpy-rpc-python-command "python")
    )
#+end_src
:END:
*** R
**** ESS
- =ess-indent-level=: Sets the number of spaces for basic indentation.
- =ess-continued-statement-offset=: Controls the offset for continued statements.
- =ess-brace-offset=: Controls the indentation of braces.
- =ess-arg-function-offset=: Adjusts indentation for arguments in function definitions.
- =ess-expression-offset=: Handles indentation for multi-line expressions.
:configuration:
#+begin_src emacs-lisp
  (use-package ess
    :straight (ess :type git
                   :files ("lisp/*.el" "doc/ess.texi" ("etc" "etc/*") ("obsolete" "lisp/obsolete/*") (
                                                                                                      :exclude "etc/other") "ess-pkg.el")
                   :host github
                   :repo "emacs-ess/ESS")
    :config
    (defun ess-insert-pipe ()
      "Insert the pipe operator |>."
      (interactive)
      (insert "|>"))
    (defun my/ess-indent-setup ()
      "Custom indentation setup for ESS."
      (setq-local ess-indent-offset 2)               ;; Main indentation level
      (setq-local ess-continued-statement-offset 2)  ;; Continuation lines
      (setq-local ess-brace-offset 0)                ;; Braces
      (setq-local ess-arg-function-offset 2)         ;; Function arguments
      (setq-local ess-expression-offset 0))          ;; Other expressions

    ;; Add '## sec:' to the beginning of the current line
    (defun my/r-tag ()
      "Add '## sec:' to the beginning of the current line if not already present."
      (interactive)
      (beginning-of-line)  ;; Move to the beginning of the line
      (unless (looking-at-p "## sec:")  ;; Check if the line already starts with '## sec:'
        (insert "## sec: ")))  ;; Insert the string if not present

    ;; Run bm-bookmark-regexp with '## sec:' as default
    (defun my/bm-r ()
      "Automatically bookmark lines matching '## sec:'."
      (interactive)
      (save-excursion
        (goto-char (point-min))
        (while (search-forward "## sec:" nil t)
  	(bm-bookmark-add))))

    ;; Customize bookmark face (bm)
    (defun my/bm-face ()
      "Customize `bm-persistent-face` in ESS mode."
      (set-face-attribute 'bm-persistent-face nil
    			:underline t
    			:background nil
    			:foreground nil))
    :hook
    (ess-mode . my/ess-indent-setup)
    (ess-mode . my/bm-face)

    :bind
    ("M--" . ess-insert-assign) ;; Arrow to when typing underscore
    ("C-|" . ess-insert-pipe)
    ("C-x ;" . my/r-tag)
    ("C-x b r" . my/bm-r)
    ) 
#+end_src
:END:
**** ESS-plot
[[[https://github.com/DennieTeMolder/ess-plot][GitHub]]] Display ESS/R plots in a dedicated Emacs window
:ess-plot:
#+begin_src emacs-lisp
  (use-package ess-plot
    :straight (ess-plot :type git
  		      :host github
  		      :repo "DennieTeMolder/ess-plot")
    :defer t)
#+end_src
:END:
**** ESS-view-data
To do tidyverse-like view and manipulate data in ESS and R. [[[https://github.com/ShuguangSun/ess-view-data][GitHub]]]
:ess-view-data:
#+begin_src emacs-lisp
  (use-package ess-view-data
    :straight (ess-view-data :type git
                               :host github
                               :repo "ShuguangSun/ess-view-data")
    :defer t
    )
#+end_src
:END:
**** Rstudio-like layout
#+begin_src emacs-lisp
  (defun my/start-r-session ()
    "Start an R session in the same directory as the current R script, without prompting."
    (interactive)
    (when (and (buffer-file-name) (string-match "\\.R$" (buffer-file-name)))
      (let ((default-directory (file-name-directory (buffer-file-name)))
            (ess-ask-for-ess-directory nil))  ;; Disable directory prompt
        (R))))

  (defun my/maximize-frame ()
    "Maximize the current Emacs frame."
    (interactive)
    (set-frame-parameter nil 'fullscreen 'maximized))

  (defun my/r4plots ()
    "Maximize the window, start R in the current directory, run `my/r4plots` to load a specific window layout."
    (interactive)
    (progn
      ;; Maximize the window if not already maximized
      (my/maximize-frame)

      ;; Start R in the same directory as the opened .R file
      (my/start-r-session)

      ;; Toggle showing plots on emacs
      (ess-plot-toggle)

      ;; Load the specified window layout using `purpose-load-window-layout`
      (purpose-load-window-layout "r4plots")
      )
    )
#+end_src
**** Rmd
:Rmarkdown:
#+begin_src emacs-lisp
  (require 'poly-R)
  (require 'poly-markdown)
#+end_src
:END:
**** Quarto
:quarto:
#+begin_src emacs-lisp
  (use-package quarto-mode
    :straight (quarto-mode
               :type git
               :flavor melpa
               :host github
               :repo "quarto-dev/quarto-emacs")
    :mode (("\\.Rmd" . poly-quarto-mode))
    )
#+end_src
:END:
*** Html
:web-beautify:
#+begin_src emacs-lisp
  (use-package web-beautify
    :straight (web-beautify :type git
                            :flavor melpa
                            :host github
                            :repo "yasuyk/web-beautify")
    :config
    (eval-after-load 'js2-mode
      '(define-key js2-mode-map (kbd "C-c b") 'web-beautify-js))
    ;; Or if you're using 'js-mode' (a.k.a 'javascript-mode')
    (eval-after-load 'js
      '(define-key js-mode-map (kbd "C-c b") 'web-beautify-js))

    (eval-after-load 'json-mode
      '(define-key json-mode-map (kbd "C-c b") 'web-beautify-js))

    (eval-after-load 'sgml-mode
      '(define-key html-mode-map (kbd "C-c b") 'web-beautify-html))

    (eval-after-load 'web-mode
      '(define-key web-mode-map (kbd "C-c b") 'web-beautify-html))

    (eval-after-load 'css-mode
      '(define-key css-mode-map (kbd "C-c b") 'web-beautify-css))
    )
#+end_src
:END:
- [[https://web-mode.org/][Webmode]]: Major mode for editing web templates
:webmode:
#+begin_src emacs-lisp
  (use-package web-mode
    :straight (web-mode :type git
                        :flavor melpa
                        :host github
                        :repo "fxbois/web-mode")
    :config
    (add-to-list 'auto-mode-alist '("\\.phtml\\'" . web-mode))
    (add-to-list 'auto-mode-alist '("\\.tpl\\.php\\'" . web-mode))
    (add-to-list 'auto-mode-alist '("\\.[agj]sp\\'" . web-mode))
    (add-to-list 'auto-mode-alist '("\\.as[cp]x\\'" . web-mode))
    (add-to-list 'auto-mode-alist '("\\.erb\\'" . web-mode))
    (add-to-list 'auto-mode-alist '("\\.mustache\\'" . web-mode))
    (add-to-list 'auto-mode-alist '("\\.djhtml\\'" . web-mode))
    (add-to-list 'auto-mode-alist '("\\.html?\\'" . web-mode))
    (setq web-mode-enable-current-column-highlight t)
    )
#+end_src
:END:
:sass:
#+begin_src emacs-lisp
  (use-package sass-mode
    :straight (sass-mode :type git
                         :flavor melpa
                         :host github
                         :repo "nex3/sass-mode")
    :config
    ;; (setq exec-path (cons (expand-file-name "") exec-path))
    )
#+end_src
:END:
:rainbow-mode:
#+begin_src emacs-lisp
  (use-package rainbow-mode
    :straight (rainbow-mode :type git
                            :host github
                            :repo "emacs-straight/rainbow-mode"
                            :files ("*" (
                                         :exclude ".git")))
    )
#+end_src
:END:
*** Latex
**** General
General configuration, Aspect, Auctex, Reftex, Synctex
:editing-customizations:
#+begin_src emacs-lisp
    ;; Optimize loading of magic-latex-buffer
  ;; Optimize loading of magic-latex-buffer and integrate pdf-tools
  (use-package magic-latex-buffer
    :straight (magic-latex-buffer :type git
                                  :flavor melpa
                                  :host github
                                  :repo "zk-phi/magic-latex-buffer")
    :init
    ;; AUCTeX configuration (related to LaTeX setup)
    (setq TeX-auto-save t                ; Enable parse on save
          TeX-parse-self t               ; Enable parse on load
          TeX-master nil                 ; Ask for the master file
          TeX-engine 'xetex              ; Use XeLaTeX by default
          reftex-plug-into-AUCTeX t      ; Integrate RefTeX with AUCTeX
          TeX-source-correlate-method 'synctex
          TeX-source-correlate-start-server t
          TeX-view-program-selection '((output-pdf "PDF Tools"))) ; Use PDF Tools for PDFs

    ;; Magic-latex-buffer customization
    (setq magic-latex-enable-block-highlight nil
          magic-latex-enable-suscript        t
          magic-latex-enable-pretty-symbols  t
          magic-latex-enable-block-align     nil
          magic-latex-enable-inline-image    nil
          magic-latex-enable-minibuffer-echo nil)

    :config
    ;; Load RefTeX when LaTeX mode is enabled
    (require 'reftex)

    ;; Use pdf-tools to open PDF files and refresh after compilation
    (add-hook 'TeX-after-compilation-finished-functions #'TeX-revert-document-buffer)

    ;; Add hooks for LaTeX mode
    :hook
    (LaTeX-mode . (lambda ()
                    (magic-latex-buffer)
                    (olivetti-mode)
                    (variable-pitch-mode)
                    (flyspell-mode)
                    (LaTeX-math-mode)
                    (turn-on-reftex)
                    (define-key LaTeX-mode-map (kbd "<C-tab>") 'outline-toggle-children)
                    (TeX-fold-mode 1)
                    (TeX-source-correlate-mode 1))))
#+end_src
:END:
**** ebib
:ebib-config:
#+begin_src emacs-lisp
  (use-package ebib
    :straight (ebib :type git
                    :host github
                    :repo "joostkremers/ebib")
    :init
    (setq
     ebib-bib-search-dirs my/ebib-search-dir
     ebib-bibtex-dialect 'biblatex
     ebib-file-associations '(("pdf") ("ps" . "gv"))
     ebib-index-columns
     '(("Author/Editor" 30 t)
       ("Title" 40 t)
       ("timestamp" 30 t)
       ("Entry Key" 20 t))
     ebib-index-default-sort '("timestamp" . descend)
     ebib-preload-bib-files my/ebib-bibtex-files
     ebib-timestamp-format "%Y-%m-%d"
     ebib-use-timestamp t))
#+end_src
:END:
*** Polymode
:polymode:
#+begin_src emacs-lisp
  (add-to-list 'auto-mode-alist '("\\.md" . poly-markdown-mode))
#+end_src
:END:
*** Terminal
Setup terminal emulators, [[https://www.emacswiki.org/emacs/AnsiTermHints][ansi-term]] and [[https://www.gnu.org/software/emacs/manual/html_mono/eshell.html][eshell]]
**** Ansi-term
#+begin_src emacs-lisp
  (defadvice ansi-term (before force-bash)
    (interactive (list my/my-term)))
  (ad-activate 'ansi-term)
  (eval-after-load "term"
    '(define-key term-raw-map (kbd "s-y") 'term-paste))
#+end_src
**** eshell
:eshell-config:
#+begin_src emacs-lisp
  (setq eshell-prompt-regexp "^[^#$\n]*[#$] "
        eshell-prompt-function
        (lambda nil
          (concat
           "[" (user-login-name) "@" (system-name) " "
           (if (string= (eshell/pwd) (getenv "HOME"))
               "~" (eshell/basename (eshell/pwd)))
           "]"
           (if (= (user-uid) 0) "# " "$ "))))
  (add-hook 'eshell-mode-hook (lambda () (setenv "TERM" "xterm-256color")))
  (global-set-key (kbd "<s-return>") 'eshell)

  ;; https://karthinks.com/software/jumping-directories-in-eshell/
  (defun eshell/z (&optional regexp)
    "Navigate to a previously visited directory in eshell, or to
  any directory proferred by `consult-dir'."
    (let ((eshell-dirs (delete-dups
                        (mapcar 'abbreviate-file-name
                                (ring-elements eshell-last-dir-ring)))))
      (cond
       ((and (not regexp) (featurep 'consult-dir))
        (let* ((consult-dir--source-eshell `(:name "Eshell"
                                                   :narrow ?e
                                                   :category file
                                                   :face consult-file
                                                   :items ,eshell-dirs))
               (consult-dir-sources (cons consult-dir--source-eshell
                                          consult-dir-sources)))
          (eshell/cd (substring-no-properties
                      (consult-dir--pick "Switch directory: ")))))
       (t (eshell/cd (if regexp (eshell-find-previous-directory regexp)
                       (completing-read "cd: " eshell-dirs)))))))
#+end_src
:END:
** Tree-sitter
[[[https://tree-sitter.github.io/tree-sitter/][Home]]]
- Install language grammar =M-x treesit-install-language-grammar=
  | Grammar    | Url                                               |
  |------------+---------------------------------------------------|
  | LaTeX      | https://github.com/latex-lsp/tree-sitter-latex    |
  | Emacs-Lisp | https://github.com/Wilfred/tree-sitter-elisp      |
  | R          | https://github.com/r-lib/tree-sitter-r            |
  | Python     | https://github.com/tree-sitter/tree-sitter-python |
  | Bash       | https://github.com/tree-sitter/tree-sitter-bash   |

#+begin_src emacs-lisp
  (use-package tree-sitter
    :straight (tree-sitter :type git
                           :files (
                                   :defaults (
                                              :exclude "lisp/tree-sitter-tests.el") "tree-sitter-pkg.el")
                           :branch "release"
                           :host github
                           :repo "emacs-tree-sitter/elisp-tree-sitter")

    :init
    (defun my-enable-tree-sitter ()
      (tree-sitter-mode)
      (tree-sitter-hl-mode))
    :hook
    ((sh-mode python-mode ess-r-mode
              css-mode js-mode) . my-enable-tree-sitter)
    )
#+end_src
*** Languages
#+begin_src emacs-lisp
  (use-package tree-sitter-langs
    :straight (tree-sitter-langs :type git
                                 :files (
                                         :defaults "queries" "tree-sitter-langs-pkg.el")
                                 :branch "release"
                                 :host github
                                 :repo "emacs-tree-sitter/tree-sitter-langs")
    )
#+end_src
*** ts-fold
#+begin_src emacs-lisp
  (use-package ts-fold
    :straight (ts-fold :type git :host github :repo "emacs-tree-sitter/ts-fold")
    :hook
    ((sh-mode python-mode
              ess-r-mode css-mode js-mode) . ts-fold-indicators-mode)
    :bind
    (("C-c ]" . ts-fold-toggle))
    )
#+end_src
* Editing
** Smart-Hungry delete
:smart-hungry:
#+begin_src emacs-lisp
  (use-package smart-hungry-delete
    :straight (smart-hungry-delete :type git
                                   :flavor melpa
                                   :host github
                                   :repo "hrehfeld/emacs-smart-hungry-delete")
    :bind
    ;; (:map python-mode-map
    ([remap backward-delete-char-untabify] . smart-hungry-delete-backward-char)
    ([remap delete-backward-char] . smart-hungry-delete-backward-char)
    ([remap delete-char] . smart-hungry-delete-forward-char)
    ;; )
    :init
    (smart-hungry-delete-add-default-hooks))
#+end_src
:END:
** Expand region, iedit
- Gradually expands selected region
:expand-region:
#+begin_src emacs-lisp
  (use-package expand-region
    :straight (expand-region :type git
                             :flavor melpa
                             :host github
                             :repo "magnars/expand-region.el")
    :config
    (global-set-key (kbd "C-=") 'er/expand-region))

  ;;Mark similar occurences and edit them at the same time
  (use-package multiple-cursors
    :straight (multiple-cursors :type git
                                :flavor melpa
                                :host github
                                :repo "magnars/multiple-cursors.el")
    :config
    (global-set-key (kbd "C-S-c C-S-c") 'mc/edit-lines)
    (global-set-key (kbd "C->") 'mc/mark-next-like-this)
    (global-set-key (kbd "C-<") 'mc/mark-previous-like-this)
    (global-set-key (kbd "C-c C-<") 'mc/mark-all-like-this)
    )

  (use-package iedit
    :straight (iedit :type git
                     :flavor melpa
                     :host github
                     :repo "victorhge/iedit"))

  (use-package wrap-region
    :straight (wrap-region :type git
                           :flavor melpa
                           :host github
                           :repo "rejeep/wrap-region.el")
    :hook
    (org-mode . wrap-region-mode)

    :config
    (wrap-region-add-wrapper "=" "=" nil 'org-mode) ; select region, hit = then region -> =region= in org-mode
    (wrap-region-add-wrapper "*" "*" nil 'org-mode) ; select region, hit * then region -> *region* in org-mode
    (wrap-region-add-wrapper "/" "/" nil 'org-mode) ; select region, hit / then region -> /region/ in org-mode
    (wrap-region-add-wrapper "_" "_" nil 'org-mode) ; select region, hit _ then region -> _region_ in org-mode
    (wrap-region-add-wrapper "+" "+" nil 'org-mode) ; select region, hit + then region -> +region+ in org-mode
    )
#+end_src
:END:
** wgrep
#+begin_src emacs-lisp
  (use-package wgrep
    :straight (wgrep :type git
                     :flavor melpa
                     :files ("wgrep.el" "wgrep-pkg.el")
                     :host github
                     :repo "mhayashi1120/Emacs-wgrep")
    :bind
    (("C-c C-p" . wgrep-change-to-wgrep-mode))
    )
#+end_src
** Undo
Vundo (visual undo) displays the undo history as a tree and lets you move in the tree to go back to previous buffer states. To use vundo, type M-x vundo RET in the buffer you want to undo. An undo tree buffer should pop up. To move around, type:
|-----------------------+-----------------------------------------------------------------|
| Key                   | Function                                                        |
|-----------------------+-----------------------------------------------------------------|
| f                     | to go forward                                                   |
| b                     | to go backward                                                  |
|                       |                                                                 |
| n                     | to go to the node below when at a branch point                  |
| p                     | to go to the node above                                         |
|                       |                                                                 |
| a                     | to go back to the last branching point                          |
| w                     | to go forward to the next branching point                       |
| e                     | to go forward to the end/tip of the branch                      |
| l                     | to go to the last saved node                                    |
| r                     | to go to the next saved node                                    |
|                       |                                                                 |
| m                     | to mark the current node for diff                               |
| u                     | to unmark the marked node                                       |
| d                     | to show a diff between the marked (or parent) and current nodes |
|                       |                                                                 |
| q                     | to quit, you can also type C-g                                  |
|                       |                                                                 |
| C-x C-s (save-buffer) | to save the buffer at the current undo state                    |
|-----------------------+-----------------------------------------------------------------|
:vundo:
#+begin_src emacs-lisp
  (use-package vundo
    :straight (vundo :type git
                     :host github
                     :repo "emacs-straight/vundo"
                     :files ("*" (:exclude ".git")))
    )
#+end_src
:END:
* Writing
** Spelling
*** Hunspell / Flyspell / Flyspell correct
Source: [[https://tex.stackexchange.com/questions/166681/changing-language-of-flyspell-emacs-with-a-shortcut][Change spelling program]]
See also this blog-post ([[http://blog.binchen.org/posts/what-s-the-best-spell-check-set-up-in-emacs/][link]]) for other form of configuration
[[http://www.emacswiki.org/emacs/FlySpell][Flyspell]] [[http://emacs.stackexchange.com/questions/13361/how-do-i-disable-the-emacs-flyspell-middle-mouse-correction][Configure flyspell buttons]]
#+begin_src emacs-lisp
   ;; Use Hunspell if available
  (when (executable-find "hunspell")
    (setq ispell-program-name "hunspell")
    ;; Ensure utf-8 encoding with Hunspell
    (with-eval-after-load 'ispell
      (defun ispell-get-coding-system () 'utf-8)))

  ;; Flyspell configuration
  (use-package flyspell
    :init
    ;; Disable sorting corrections and suppress message flag for flyspell
    (setq flyspell-sort-corrections nil
          flyspell-issue-message-flag nil)
    ;; Enable flyspell in specific modes
    (dolist (hook '(text-mode-hook))
      (add-hook hook #'flyspell-mode))
    ;; Disable flyspell in specific modes
    (dolist (hook '(change-log-mode-hook log-edit-mode-hook))
      (add-hook hook (lambda () (flyspell-mode -1)))))

  ;; Flyspell-correct configuration
  (use-package flyspell-correct
    :straight (flyspell-correct :type git
                                :flavor melpa
                                :files ("flyspell-correct.el" "flyspell-correct-ido.el" "flyspell-correct-pkg.el")
                                :host github
                                :repo "d12frosted/flyspell-correct")
    :after flyspell
    :bind
    (:map flyspell-mode-map ("C-;" . flyspell-correct-wrapper)))
#+end_src
** Thesaurus
For quick access to specific query types, use these commands:
- =powerthesaurus-lookup-synonyms-dwim=
- =powerthesaurus-lookup-antonyms-dwim=
- =powerthesaurus-lookup-related-dwim=
- =powerthesaurus-lookup-definitions-dwim=
- =powerthesaurus-lookup-sentences-dwim=
:power-thesaurus:
#+begin_src emacs-lisp
  (use-package powerthesaurus
   :straight (powerthesaurus :type git
                       :flavor melpa
                       :host github
                       :repo "SavchenkoValeriy/emacs-powerthesaurus"))
#+end_src
:END:
** Lorem ipsum
#+begin_src emacs-lisp
  (use-package lorem-ipsum
  :straight (lorem-ipsum :type git
                         :flavor melpa
                         :host github
                         :repo "jschaf/emacs-lorem-ipsum"))
#+end_src
* Remote
** Version control
*** diff-hl
#+begin_src emacs-lisp
  (use-package diff-hl
    :straight (diff-hl :type git
                       :host github
                       :repo "dgutov/diff-hl")
    :hook
    ((prog-mode . diff-hl-mode)          ;; Enable diff-hl in programming modes
     (magit-post-refresh . diff-hl-magit-post-refresh))  ;; Refresh diff-hl after Magit operations
    :config
    (setq diff-hl-draw-borders nil)
    )
#+end_src
*** Git timemachine
Step through historic versions of git controlled files. [[[https://codeberg.org/pidu/git-timemachine][Home]]]
Use the following keys to navigate historic version of the file
- =p= Visit previous historic version
- =n= Visit next historic version
- =w= Copy the abbreviated hash of the current historic version
- =W= Copy the full hash of the current historic version
- =g= Goto nth revision
- =t= Goto revision by selected commit message
- =q= Exit the time machine.
- =b= Run magit-blame on the currently visited revision (if magit available).
- =c= Show current commit using magit (if magit available).

#+begin_src emacs-lisp
  (use-package git-timemachine
    :straight (git-timemachine :type git
                               :flavor melpa
                               :host codeberg
                               :repo "pidu/git-timemachine")

    )
#+end_src
** TRAMP
:tramp:
#+begin_src emacs-lisp
  (setq tramp-default-method "ssh")
#+end_src
:END:
* Others
** yequake-frames
#+begin_src emacs-lisp
  (use-package yequake
    :straight (yequake :type git
                       :flavor melpa
                       :host github
                       :repo "alphapapa/yequake")
    :init
    (get-buffer-create "tmp-notes")
    :config
    (setq yequake-frames
          '(("tmp-notes" .
             ((width . 0.45)
              (height . 0.45)
              (alpha . 0.85)
              (buffer-fns . ("tmp-notes"))
              (frame-parameters . ((undecorated . t)
                                   (menu-bar-lines . 0)
                                   ))
              ))))
    )
#+end_src
